# Server configuration
# Node environment: development | production
# NODE_ENV=production

# Public server port
# PORT=3000

# If the client is hosted on a different origin in production,
# set the allowed origin for Socket.IO CORS (e.g. https://game.example.com)
# CLIENT_ORIGIN=

# Client configuration (Vite)
# When hosting client and server on different origins, set the Socket.IO URL.
# Example: VITE_SOCKET_URL=https://api.example.com
# VITE_SOCKET_URL=

